import styled from "styled-components";
import { font } from "../styles/Common";


export const SectionTitle = styled.h2`
    ${font({ weight: 700, Fmax: 40, Fmin: 30 })}

    text-align: center;
    letter-spacing: 1px;
    margin-bottom: 55px;
`