import styled from "styled-components";

const Slogans = styled.section`
`

const Slogan = styled.div`
    font-size: 18px;
    font-weight: 700;
    padding: 40px;
`
const Text = styled.p`
    
`
export const S = {
    Slogans,
    Slogan,
    Text
}